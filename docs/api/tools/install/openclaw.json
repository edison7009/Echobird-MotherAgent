{
    "id": "openclaw",
    "displayName": "OpenClaw",
    "homepage": "https://openclaw.ai/",
    "docs": "https://docs.openclaw.ai/",
    "github": "https://github.com/openclaw/openclaw",
    "install": {
        "npm (recommended)": "npm install -g openclaw@latest && openclaw onboard --install-daemon",
        "npm no-onboard (remote SSH — PREFERRED for remote servers)": "npm install -g openclaw@latest",
        "pnpm": "pnpm add -g openclaw@latest && openclaw onboard --install-daemon",
        "npm China mirror (slow network fallback)": "npm install -g openclaw@latest --registry=https://registry.npmmirror.com",
        "from source": "git clone https://github.com/openclaw/openclaw.git && cd openclaw && pnpm install && pnpm build",
        "note": "Requires Node 22+ (LTS recommended). For REMOTE servers via SSH, skip --onboard to avoid interactive prompts. The onboard step installs the Gateway daemon (launchd/systemd)."
    }
}