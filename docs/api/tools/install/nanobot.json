{
    "id": "nanobot",
    "displayName": "NanoBot",
    "homepage": "https://github.com/HKUDS/nanobot",
    "docs": "https://github.com/HKUDS/nanobot",
    "github": "https://github.com/HKUDS/nanobot",
    "install": {
        "pip (stable)": "pip install nanobot-ai",
        "uv (stable, fast)": "uv tool install nanobot-ai",
        "from source (latest)": "git clone https://github.com/HKUDS/nanobot.git && cd nanobot && pip install -e .",
        "WARNING": "The PyPI package is 'nanobot-ai', NOT 'nanobot'. The 'nanobot' package on PyPI is a completely different project (robot navigation framework).",
        "IMPORTANT": "NanoBot is NOT NanoClaw. NanoClaw (https://github.com/qwibitai/nanoclaw, https://nanoclaw.dev/) is a completely different project — a Claude Code wrapper. EchoBird does NOT support NanoClaw. Do NOT install NanoClaw unless the user explicitly asks for it by name.",
        "note": "Python-based agent by HKUDS (Hong Kong University). After install, verify with: nanobot --version"
    },
    "cli": {
        "description": "NanoBot uses cli-oneshot protocol for EchoBird integration. The agent itself runs persistently via 'nanobot gateway'.",
        "chat_command": "nanobot agent -m \"<message>\"",
        "detect_command": "nanobot --version",
        "gateway_command": "nanobot gateway",
        "supported_flags": ["-m (message)", "--no-logs (suppress runtime logs)", "--session/-s (session ID)", "--workspace/-w (custom workspace)", "--config/-c (config path)"],
        "role_mechanism": "NanoBot uses SOUL.md file at ~/.nanobot/workspace/SOUL.md for agent persona. EchoBird writes role content here; NanoBot auto-detects changes. Note: AGENTS.md does NOT work for identity.",
        "output_format": "Clean plain text with cat emoji (U+1F408) delimiter. Output is very clean by default (no ANSI codes or log lines when using --no-logs).",
        "note": "NanoBot is Python-based (unlike PicoClaw which is Go). Config format uses 'providers' object, NOT 'model_list' array."
    },
    "network_requirements": {
        "test_urls": ["https://pypi.org/simple/nanobot-ai/"],
        "mirrors": {
            "pip": "pip install nanobot-ai -i https://pypi.tuna.tsinghua.edu.cn/simple",
            "uv": "uv tool install nanobot-ai --index-url https://pypi.tuna.tsinghua.edu.cn/simple"
        }
    }
}
